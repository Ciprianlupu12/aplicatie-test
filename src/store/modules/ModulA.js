const state = {
    commonVar: '',
};
const getters = {
    commonVar:state => state.commonVar
};
const mutations = {
};
const actions = {
};

export default{
    state,
    getters,
    mutations,
    actions
}